workflow:
  name: "default"
  description: "빡빡한 워크플로우: spec → design → implement"

steps:
  - spec
  - design
  - implement

output:
  path: ".claude/siat/specs"

execution:
  mode: "manual"  # "manual" | "auto"

hooks:
  pre-step: []
    # - agent:navigator
  post-step: []
    # - agent:reporter
