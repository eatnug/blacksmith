workflow:
  name: "default"
  description: "기본 워크플로우: plan → implement"

steps:
  - plan
  - implement

output:
  path: ".claude/siat/specs"

execution:
  mode: "manual"  # "manual" | "auto"

hooks:
  pre-step: []
    # - agent:navigator
  post-step: []
    # - agent:reporter
