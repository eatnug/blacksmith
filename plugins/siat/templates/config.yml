workflow:
  name: "default"
  description: "체계적인 워크플로우: ideation → spec → design → implement"

steps:
  - ideation
  - spec
  - design
  - implement

output:
  path: ".claude/siat/specs"

execution:
  mode: "manual"  # "manual" | "auto"

hooks:
  pre-step: []
    # - agent:navigator
  post-step: []
    # - agent:reporter
