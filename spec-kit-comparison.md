# Spec Kit vs Siat 비교 및 개선 방향

## Spec Kit 개요

GitHub에서 만든 Spec-Driven Development 툴킷. "코드가 아닌 명세를 일급 객체로" 하는 개발 방식.

### 워크플로우

```
Constitution → Specify → Plan → Tasks → Implement
(원칙 정의)    (요구사항)  (설계)  (작업분해)  (구현)
```

### 핵심 철학

> "Specifications don't serve code—code serves specifications."

- 문서가 실행 가능한 소스가 됨
- 12시간 문서 작업 → 15분 자동화
- 개발자는 코딩이 아닌 **조종(Steer)** 역할

---

## Spec Kit에서 배울 점

### 1. Constitution (프로젝트 원칙)

모든 스텝에서 참조되는 **전역 원칙** 정의.

```markdown
# .claude/siat/constitution.md

## 원칙
1. 테스트 없이 구현 없다
2. 모든 API는 REST + OpenAPI 스펙 필수
3. 외부 의존성 추가 시 반드시 사유 문서화

## 금지
- 기존 인터페이스 breaking change
- 환경변수 하드코딩
```

**효과**: 프로젝트 일관성 확보, AI가 원칙에 맞지 않는 제안 방지

---

### 2. [NEEDS CLARIFICATION] 마커

AI가 **추측하지 않고 명시적으로 불명확한 부분 표시**.

```markdown
# spec.md 작성 시

## 요구사항
- 사용자 로그인 기능
- 소셜 로그인 지원 [NEEDS CLARIFICATION: Google만? Apple도?]
- 세션 유지 시간 [NEEDS CLARIFICATION: 얼마나?]
```

**규칙**:
- 최대 3개까지만 허용
- 우선순위: scope > security > UX > technical
- 해결 전까지 다음 단계 진행 불가

---

### 3. Constitution Check (게이트)

다음 단계 진입 전 **검증 게이트**.

```markdown
# design.md 시작 전

## Constitution Check
- [ ] spec에 불명확한 부분 없음
- [ ] 모든 [NEEDS CLARIFICATION] 해결됨
- [ ] 프로젝트 원칙과 충돌 없음

## Gate Check
- [ ] 모든 요구사항이 구체적이고 측정 가능
- [ ] 수용 기준이 정의됨
- [ ] 범위가 명확함
```

**효과**: 부실한 산출물로 다음 단계 진행 방지

---

### 4. Task 구조 개선

```markdown
## Tasks

[T001] [P] [US1] tests/auth.test.ts 작성
[T002] [P] [US1] src/auth/login.ts 구현
[T003]     [US1] src/auth/session.ts 구현 (T002 의존)
[T004] [P] [US2] src/profile/edit.ts 구현

의존성: T003 → T002
병렬 가능: T001, T002, T004
```

**구성요소**:
- `[T###]` - 고유 ID
- `[P]` - 병렬 실행 가능 마커
- `[US#]` - 사용자 스토리 연결
- 의존성 명시

**효과**: 작업 추적성, 병렬화 최적화

---

### 5. Phase 분류

큰 작업을 단계별로 분류:

| Phase | 설명 | 예시 |
|-------|------|------|
| **Setup** | 환경 설정 | 패키지 설치, 설정 파일 |
| **Foundation** | 공통 인프라 (블로킹) | DB 스키마, 공통 유틸 |
| **Stories** | 사용자 스토리별 (병렬 가능) | 각 기능 구현 |
| **Polish** | 마무리 | 리팩토링, 문서화 |

---

### 6. 추상화 레벨 강제

| 단계 | 허용 | 금지 |
|------|------|------|
| **Spec** | WHAT, WHY, 사용자 관점 | HOW, 기술 용어, 프레임워크 |
| **Design** | HOW, 인터페이스, 아키텍처 | 구현 코드 |
| **Implement** | 코드, 테스트 | 스코프 변경, 설계 변경 |

**예시**:
- Spec: "사용자가 1초 안에 결과를 본다" ✅
- Spec: "API 응답시간 200ms 이하" ❌ (기술적 → Design에서)

---

## Siat vs Spec Kit 비교

| 항목 | Spec Kit | Siat (현재) | Siat (개선) |
|------|----------|-------------|-------------|
| 전역 원칙 | constitution.md | ❌ | ✅ 추가 |
| 불명확 마커 | [NEEDS CLARIFICATION] | ❌ | ✅ 추가 |
| 품질 게이트 | constitution check | ❌ | ✅ gate.md 추가 |
| Task 구조 | ID, [P], 의존성 | 단순 | ✅ 개선 |
| 스텝 커스터마이징 | ⚠️ 제한적 | ✅ 자유 | 유지 |
| 훅 시스템 | ❌ | ✅ | 유지 |
| 도구 호환성 | CLI로 멀티 지원 | Claude Code 전용 | 향후 개선 |

---

## Siat 개선 적용 계획

### 디렉토리 구조

```
.claude/siat/
├── constitution.md          ← 🆕 추가
├── config.yml
├── steps/
│   ├── spec/
│   │   ├── instruction.md   ← 수정: NEEDS CLARIFICATION 강제
│   │   ├── spec.md          ← 수정: 마커 섹션 추가
│   │   └── gate.md          ← 🆕 다음 단계 진입 조건
│   ├── design/
│   │   ├── instruction.md   ← 수정: constitution check 추가
│   │   ├── spec.md
│   │   └── gate.md          ← 🆕
│   └── implement/
│       ├── instruction.md   ← 수정: task 구조 개선
│       ├── spec.md
│       └── gate.md          ← 🆕
```

### 우선순위

| 순위 | 항목 | 효과 | 난이도 |
|-----|------|------|-------|
| 1 | Constitution | 프로젝트 일관성 | 낮음 |
| 2 | [NEEDS CLARIFICATION] | AI 추측 방지 | 낮음 |
| 3 | Gate (검증 체크리스트) | 품질 게이트 | 중간 |
| 4 | Task 구조 개선 | 병렬화, 추적성 | 중간 |
| 5 | Phase 분류 | 큰 프로젝트용 | 낮음 |

---

## 참고 자료

- [GitHub Spec Kit Repository](https://github.com/github/spec-kit)
- [GitHub Blog - Spec-driven development with AI](https://github.blog/ai-and-ml/generative-ai/spec-driven-development-with-ai-get-started-with-a-new-open-source-toolkit/)
- [Martin Fowler - Understanding SDD](https://martinfowler.com/articles/exploring-gen-ai/sdd-3-tools.html)
